---
layout: article
title: AACSearch MCP and OpenCode
description: Learn how to add the AACSearch MCP servers to OpenCode to interact with both the AACSearch API and documentation.
---

Learn how you can add the AACSearch MCP servers to OpenCode to interact with both the AACSearch API and documentation.

Before you begin, ensure you have the following **pre-requisites** installed on your system:

{% tabs %}
{% tabsitem #api-server-prerequisites title="API server" %}

[uv](https://docs.astral.sh/uv/getting-started/installation/) must be installed on your system.

{% /tabsitem %}

{% tabsitem #docs-server-prerequisites title="Docs server" %}

[Node.js](https://nodejs.org/en/download) and npm must be installed on your system.

{% /tabsitem %}
{% /tabs %}

{% section #step-1 step=1 title="Add MCP servers" %}

Use the following configuration in your `opencode.json` file to use the AACSearch MCP servers.

{% tabs %}
{% tabsitem #api-only title="API server" %}

```json
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "aacsearch": {
      "type": "local",
      "command": [
        "uvx",
        "mcp-server-aacsearch",
        "--sites"
      ],
      "enabled": true,
      "environment": {
        "AACSEARCH_PROJECT_ID": "your-project-id",
        "AACSEARCH_API_KEY": "your-api-key",
        "AACSEARCH_ENDPOINT": "https://<REGION>.cloud.aacsearch.io/v1"
      }
    }
  }
}
```

**Configuration:**
- Replace `your-project-id` with your actual AACSearch project ID
- Replace `your-api-key` with your AACSearch API key
- Replace `<REGION>` with your AACSearch Cloud region (e.g., `nyc`, `fra`)

{% /tabsitem %}

{% tabsitem #docs-only title="Docs server" %}

```json
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "aacsearch-docs": {
      "type": "remote",
      "enabled": true,
      "url": "https://mcp-for-docs.aacsearch.io"
    }
  }
}
```

{% /tabsitem %}
{% /tabs %}

{% info title="Enable other API MCP tools" %}

To enable additional API tools, learn more about [command-line arguments](/docs/tooling/mcp/api#command-line-arguments).

{% /info %}

{% /section %}

{% section #step-2 step=2 title="Test the integration" %}

Try out the following example prompts based on the MCP server you have configured:

{% tabs %}
{% tabsitem #test-api title="API server" %}

**Example prompts:**
- `Create a new user in my AACSearch project`
- `List all databases in my project`
- `Show me the collections in my database`
- `Create a new document in my collection`
- `Delete a specific user by ID`

{% /tabsitem %}

{% tabsitem #test-docs title="Docs server" %}

**Example prompts:**
- `How do I set up real-time subscriptions in AACSearch?`
- `Show me how to authenticate users with OAuth`
- `What are the best practices for database queries?`
- `How do I implement file uploads with AACSearch Storage?`
- `Show me an example of using AACSearch Functions`

{% /tabsitem %}

{% /tabs %}

![Implement OAuth authentication in AACSearch](/images/docs/mcp/opencode/oauth-question.png)

{% /section %}